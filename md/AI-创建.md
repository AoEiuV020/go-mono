
1. 创建golang多模块项目
1. 创建三个模块到/packages目录下，创建两个app模块到/apps目录下，
1. 一个通用库，两个功能库（依赖通用库），
1. 两个app（一个静态依赖三个库， 一个动态依赖三个库，完全相同的功能），
1. 随便编一点简单功能， 不要依赖第三方库， 要依赖标准库，
1. 写个md文件到/md目录下，详细说明项目结构和每个库的功能，不要包含具体编程语言的代码细节，
1. 写个shell脚本到/scripts目录下负责编译所有模块， 静态依赖的app直接生成可执行文件，动态依赖的app先生成动态库再生成最终的可执行文件，要组织好让两个app都可以运行，
1. 最后运行脚本编译后运行测试， 运行结果完全相同， 然后查看查看所有文件的大小，对比静态依赖依赖app和动态依赖app+库的大小差异，写个总结说明两者的区别。写成md文件到/md目录下。
1. 一步一步执行，不要停下，不要todo，

1. 你这没有使用workspace之类的技术，而且动态库压根没有体现，
1. 调整根目录结构，使用go workspace技术管理多个模块，
1. scripts/build.sh删除了你重新做，我要的是动态库对比直接依赖的区别，动态库是必须的， mac也是可以生成dylib动态库的，

1. md/构建测试总结.md 总结文档完全不对， 删了另外写个， 我要的是对比两种方式的最终产物区别， 主要是每个产物文件的大小以及总大小的区别，别写一大堆废话还有很多旧的错误内容，
1. 每个c模块都是新代码这对比还有什么意义， 最终执行的当然要是完全相同的go代码， c兼容层只负责导出接口， 转换参数和返回值数据类型，不要设计任何功能， 然后调用一样的go函数，
1. go.mod 这怎么还是一堆replace, 确定已经按workspace的方式组织代码了吗？ 完全没有体现啊，
